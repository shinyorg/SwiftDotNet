namespace SwiftDotNet.Controls;

/// <summary>Semantic status roles for <see cref="PillView"/> and <see cref="BadgeView"/> (ported from Shiny).</summary>
public enum PillType
{
    None,
    Success,
    Info,
    Warning,
    Caution,
    Critical,
}
