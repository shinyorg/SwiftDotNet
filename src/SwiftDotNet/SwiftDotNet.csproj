<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Base (platform-neutral Core). Apple TFMs need a Mac; the Windows TFM needs Windows;
         Android builds anywhere. Linux/GTK stays a separate project (it shares the net10.0 TFM). -->
    <!-- netstandard2.1 is the Unity target: Unity 6's scripting runtime is netstandard2.1, and CoreCLR
         is not something to build on yet. It carries no platform code — see docs/backends/unity.md. -->
    <TargetFrameworks>net10.0;net10.0-android;netstandard2.1</TargetFrameworks>
    <TargetFrameworks Condition="$([MSBuild]::IsOSPlatform('OSX'))">$(TargetFrameworks);net10.0-ios;net10.0-macos;net10.0-tvos</TargetFrameworks>
    <TargetFrameworks Condition="$([MSBuild]::IsOSPlatform('Windows'))">$(TargetFrameworks);net10.0-windows10.0.19041.0</TargetFrameworks>

    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <LangVersion>latest</LangVersion>
    <RootNamespace>SwiftDotNet</RootNamespace>
    <!-- Only the modern TFMs understand trimming; netstandard2.1 (the Unity retarget) warns NETSDK1212 if
         this is set unconditionally. The condition is the SDK's own suggested form. -->
    <IsTrimmable Condition="$([MSBuild]::IsTargetFrameworkCompatible('$(TargetFramework)', 'net8.0'))">true</IsTrimmable>
  </PropertyGroup>

  <!-- Per-platform OS versions. Conditioned on $(TargetFramework) (a global property, set during property
       evaluation) rather than $(TargetPlatformIdentifier), which the SDK only defines in its bottom targets
       import — too late for a PropertyGroup condition, so those would silently never apply. -->
  <PropertyGroup Condition="$(TargetFramework.EndsWith('-ios'))"><SupportedOSPlatformVersion>17.0</SupportedOSPlatformVersion></PropertyGroup>
  <PropertyGroup Condition="$(TargetFramework.EndsWith('-tvos'))"><SupportedOSPlatformVersion>17.0</SupportedOSPlatformVersion></PropertyGroup>
  <PropertyGroup Condition="$(TargetFramework.EndsWith('-macos'))"><SupportedOSPlatformVersion>14.0</SupportedOSPlatformVersion></PropertyGroup>
  <PropertyGroup Condition="$(TargetFramework.EndsWith('-android'))"><SupportedOSPlatformVersion>24</SupportedOSPlatformVersion></PropertyGroup>
  <PropertyGroup Condition="$(TargetFramework.Contains('-windows'))">
    <SupportedOSPlatformVersion>10.0.17763.0</SupportedOSPlatformVersion>
    <UseWinUI>true</UseWinUI>
  </PropertyGroup>

  <!-- Core (Core/**) compiles for every TFM. Platform code is opted in per TFM. -->
  <ItemGroup>
    <Compile Remove="Platforms/**/*.cs" />
  </ItemGroup>

  <!-- Unity target: hot reload rides on System.Reflection.Metadata.MetadataUpdateHandler, which does not
       exist before .NET 5. Unity has its own domain-reload story, so the file is simply excluded rather
       than stubbed into something that would silently never fire. -->
  <ItemGroup Condition="'$(TargetFramework)' == 'netstandard2.1'">
    <Compile Remove="Core/HotReload.cs" />
  </ItemGroup>

  <!-- Hosting/DI (Core/Hosting/**). Taken by the root library on purpose: SwiftProgram.CreateSwiftApp()
       is the framework's front door, and the UseX() seam has to live where the sibling libraries
       (Controls, Maps, Camera, Skia) can extend it. The render path stays free of these types — it binds
       only to BCL IServiceProvider via SwiftHost. -->
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="10.0.*" />
    <PackageReference Include="Microsoft.Extensions.Logging" Version="10.0.*" />
  </ItemGroup>
  <ItemGroup Condition="'$(TargetPlatformIdentifier)' == 'ios'">
    <Compile Include="Platforms/iOS/**/*.cs" />
  </ItemGroup>
  <ItemGroup Condition="'$(TargetPlatformIdentifier)' == 'macos'">
    <Compile Include="Platforms/macOS/**/*.cs" />
  </ItemGroup>
  <ItemGroup Condition="'$(TargetPlatformIdentifier)' == 'tvos'">
    <Compile Include="Platforms/tvOS/**/*.cs" />
  </ItemGroup>
  <ItemGroup Condition="'$(TargetPlatformIdentifier)' == 'android'">
    <Compile Include="Platforms/Android/**/*.cs" />
  </ItemGroup>
  <!-- TFM-string conditioned, not $(TargetPlatformIdentifier): that property defaults to 'windows' for a
       non-.NET5+ TFM, which would compile the whole WinUI backend into the netstandard2.1 (Unity) build. -->
  <ItemGroup Condition="$(TargetFramework.Contains('-windows'))">
    <Compile Include="Platforms/Windows/**/*.cs" />
  </ItemGroup>

  <!-- Apple (iOS/macOS/tvOS): the Swift/SwiftUI bridge NativeReference (internal ItemGroup is TFM-gated). -->
  <Import Project="SwiftDotNetBridge.targets" />

  <!-- Android: the Compose bridge AAR + Compose runtime.

       These versions are not free-floating: they have to agree with what .NET MAUI's AndroidX graph
       asks for, because the Skia MAUI host (SwiftDotNet.Skia.Maui) puts both in one app. MAUI pulls
       Xamarin.AndroidX.Activity.Ktx >= 1.10.1.3 (via Navigation.Fragment → Fragment.Ktx), and
       Activity.Compose pins Activity.Ktx to an exact minor — so an Activity.Compose older than 1.10.1
       makes the two graphs unsatisfiable (NU1107) and Android can host Compose or MAUI, never both.
       Keep Activity.Compose >= 1.10.1.x when bumping. -->
  <ItemGroup Condition="'$(TargetPlatformIdentifier)' == 'android'">
    <AndroidLibrary Include="$(MSBuildThisFileDirectory)..\..\build\SwiftDotNetComposeBridge.aar" Bind="true" />
    <PackageReference Include="Xamarin.AndroidX.Compose.Ui" Version="1.11.4" />
    <PackageReference Include="Xamarin.AndroidX.Compose.Foundation" Version="1.11.4" />
    <PackageReference Include="Xamarin.AndroidX.Compose.Material3" Version="1.4.0.3" />
    <PackageReference Include="Xamarin.AndroidX.Activity.Compose" Version="1.13.0.1" />
  </ItemGroup>

  <!-- Windows: WinUI 3 / Windows App SDK.

       Conditioned on the TFM string rather than $(TargetPlatformIdentifier): for a non-.NET5+ TFM such as
       netstandard2.1 the SDK defaults that property to 'windows', which silently pulled the Windows App
       SDK into the Unity target and failed its "requires .NET 6.0" check. -->
  <ItemGroup Condition="$(TargetFramework.Contains('-windows'))">
    <PackageReference Include="Microsoft.WindowsAppSDK" Version="1.7.*" />
  </ItemGroup>

</Project>
